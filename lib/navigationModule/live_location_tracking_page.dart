import 'package:flutter/material.dart';

class LiveLocationTrackingPage extends StatelessWidget {
  const LiveLocationTrackingPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        backgroundColor: const Color(0xFF2561FA),
        foregroundColor: Colors.white,
        title: const Text('Live Location Tracking'),
      ),
      body: const Center(
        child: Text('Live Location Tracking Feature Coming Soon'),
      ),
    );
  }
} 